<Window x:Class="DnDCharacterSheetTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DnD_3._5_Character_Sheet_Tool"
        xmlns:resx="clr-namespace:DnDCharacterSheetTool.Properties"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        Loaded="Window_Loaded"
        Title="{x:Static resx:Resources.CharacterSheetTool}" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="UnderScoreOnlyTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Frame x:Name="MainFrame"/>
    </Grid>
</Window>
